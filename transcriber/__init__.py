"""Local meeting/audio transcriber: record, fetch, transcribe, organize.

The ASGI app lives in ``transcriber.app`` (``create_app()`` factory plus a
module-level ``app`` for ``uvicorn transcriber.app:app``).
"""
